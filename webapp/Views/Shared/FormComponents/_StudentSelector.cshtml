@model webapp.Models.ViewModels.StudentListViewModel;
@if (Model.Students.Count < 1)
{
    <!option>No eligible students</!option>
}
else
{
    @foreach (var student in Model.Students)
    {
        <!option value="@student.Email">@student.GivenName @student.LastName</!option>
    }
}
