@model webapp.Models.ViewModels.RolesOptionsViewModel;
<select class="role-select" name="Role">
    @foreach (var role in Model.Roles)
    {
        <!option value="@role" @(role == "Instructor" ? "selected" : "")>@role</!option>
    }
</select>
